/**
 * JsonEdge interface.
 * @author Aleksi Huotala
 */
interface JsonEdge {
  /**
   * Target node id
   */
  i: number;
  /**
   * Weight of the edge
   */
  w: number;
}

export default JsonEdge;
